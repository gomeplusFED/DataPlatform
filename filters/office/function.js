/**
 * @author yanglei
 * @date 2017-02-07
 * @fileoverview
 */
module.exports = {
    one(data, query, dates) {
        const source = data.first.data[0],
            type_filter = query.main_show_type_filter,
            map = {},
            type = "line";

        if(type !== "table") {

        } else {
            let newData = {};
            for(let date of dates) {
                
            }
        }
    }
};